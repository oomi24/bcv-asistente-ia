import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bcv.asistenteia',
  appName: 'bcv-asistente-de-contenido-ia',
  webDir: 'dist'
};

export default config;
